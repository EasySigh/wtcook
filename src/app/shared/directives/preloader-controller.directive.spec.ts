import { PreloaderControllerDirective } from './preloader-controller.directive';

describe('PreloaderControllerDirective', () => {
  it('should create an instance', () => {
    const directive = new PreloaderControllerDirective();
    expect(directive).toBeTruthy();
  });
});
